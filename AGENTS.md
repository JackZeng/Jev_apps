# 仓库协作约定

本仓库是 Jev 应用案例与原理拆解的中文知识库。默认使用 Markdown 和中文正文，不引入网站框架、数据库或构建依赖，除非用户明确要求。

- 新线索进入 `inbox/README.md`；正式案例和拆解使用 `templates/` 中的模板。
- 新增或修改条目时同步相应索引，使用仓库内相对链接。
- 目录日期为收录日期，原帖发布日期单独记录；不知道的信息写“未知”。
- 不编造案例、来源、作者、日期、产品能力或实验结果。
- 分清官方说明、作者陈述、实际观察、复现实验与技术推测。
- 未实际运行不得标记“已复现”；未实际复查不得更新复查日期。
- 相同案例尽量合并来源；失效内容注明状态并保留有价值的历史记录。
- 第三方附件注明来源和使用依据，不提交凭据、私人内容或大型原始素材。
- 完成编辑后检查相对链接、索引同步和 `git diff --check`。纯文档改动无需添加测试框架。
- 用户仅要求初始化时，搭建结构与模板，不将占位示例伪装成真实收录。

## X 案例目录维护

- 正式案例主帖取数时至少 200 赞，记录取数时间和渠道，附对应图片或视频；补充帖点赞不累计。
- `data/catalog.json` 是首批案例和专题对比的编辑来源；生成内容不要单独手改。
- 执行 `python3 scripts/build_catalog.py` 后，用 `--check` 检查同步，再检查相对链接与 `git diff --check`。
- 相同项目合并更新帖；独立实现按相同用途归组并对比适用场景和限制。
- README 必须包含所有正式案例的简介、点赞来源、媒体预览与详情链接。
- 原始研究材料仅放在忽略的 `local/`；公开目录不保存完整社交媒体帖文。
